import { StyleSheet } from 'react-native';

const ExStyles = StyleSheet.create({
    textBox: {
        color: 'pink',
        fontSize: 30,
        backgroundColor: 'red',
    },
});

export default ExStyles;
